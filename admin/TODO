-real maxima script
	-done! -batch mode
	-done! -user preferences (par
	-interface to external programs
	-docs
		-man page
		-done! -dynamic (--help)
	-interface for invoking tests
	-mute the clisp banner, etc. (partially done)

-done! -create test directory

-done! -make dependencies for defsystem

-install emacs files
	-options for emacs, xemacs

-install packages

-install docs

-install xmaxima
	-option to not install

-spec files
	-separate packages for base, each lisp impl, emacs, xemacs...

-configure options
	-fix default lisp
	-optional paths to specific lisps

-write PORTING documentation
	-do while adding acl support