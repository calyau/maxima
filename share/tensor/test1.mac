(if get('ctensor,'version) = false then load(ctensor),done)$
(setflags(),ratfac:true,dim:4,omega:[x,y,z,t],
 lg:
 matrix([1/(1-2*m/x),0,0,0],[0,x^2,0,0],[0,0,x^2*sin(y)^2,0],[0,0,0,2*m/x-1]))$
metric()$
christof(mcs)$
riccicom(true)$
scurvature();
riemann(true)$
raiseriemann(false)$
rinvariant();
